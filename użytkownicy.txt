Użytkownicy bazy danych:

1. Gość
CREATE USER StdUser IDENTIFIED BY "Default_StdUser_Password321";
CREATE USER PremiumUser IDENTIFIED BY "Default_PremiumUser_Password321";
CREATE USER SuperAdmin IDENTIFIED BY "Default_SuperAdmin_Password321";
CREATE USER Guest IDENTIFIED BY "Default_Guest_Password321";

+ SELECT z tabel użytkownicy
+ INSERT do tabeli użytkownicy, 
- cała reszta

2. Użytkownik_basic
+ SELECT z wszystkich tabel oprócz ulubione i historia roczna
+ UPDATE zamówień, użytkowników, 
+ INSERT zamówień
+ DELETE zamówień
- cała reszta

3. Użytkownik_premium
+ to co Użytkownik_basic
+ SELECT ulubione oraz historia roczna
+ UPDATE ulubione
+ INSERT ulubione
+ DELETE ulubione
- cała reszta

4. Admin
+ wszystko
- DROP